(function (name) {
    console.log('Hello' + name);
 } )('hi');
     
 
// (function(len,breath){

//     const area = len * breath;
//     console.log('the are is '+ area);

// })(1,2); 


((len,brea) =>{

    const area = len * brea;
    console.log('the are is '+ area);
})(1,2);